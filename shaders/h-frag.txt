#include "preamble"

uniform vec2 spadDist;   // Distance between spad points

varying vec3 vColor;

void main() {
    //gl_FragColor = vec4(vColor, 1.0);
    gl_FragColor = vec4((vColor.r+vColor.g+vColor.b)/3.0,0.0,0.0, 1.0);
    //gl_FragColor = vec4(spadDist, 0.0, 1.0);
}
